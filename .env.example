# =============================================================================
# Sata Robo VN — biến môi trường (MẪU). Copy sang .env.local (KHÔNG commit).
# -----------------------------------------------------------------------------
# ⚠️  ĐỂ BẢN DEPLOY (Vercel / GitHub Actions) CHẠY GIỐNG LOCALHOST:
#     Phải set ĐẦY ĐỦ các biến 🔴 dưới đây trên môi trường deploy:
#       • Vercel → Project → Settings → Environment Variables (Production + Preview)
#       • hoặc GitHub → Settings → Secrets and variables → Actions
#         (nếu deploy bằng .github/workflows/deploy.yml)
#     ➜ THIẾU FEATURE FLAG = tính năng (Portal V2, SCORM, Eval…) TỰ TẮT trên bản
#       deploy dù localhost vẫn thấy. Đây là lý do phổ biến nhất "deploy ≠ local".
#     Xem chi tiết: Document → Triển khai → Deployment (Vercel).
# =============================================================================

# ─── Database (Supabase) — 🔴 BẮT BUỘC ───────────────────────────────────────
# Supabase direct :5432 chỉ có IPv6 → LUÔN dùng pooler (xem .claude/rules/prisma-db.md):
#   • Runtime  (DATABASE_URL): transaction pooler :6543
#   • Migrate  (DIRECT_URL):   session pooler     :5432
# Username dạng postgres.<project-ref> (KHÔNG phải "postgres").
DATABASE_URL="postgresql://postgres.<ref>:[password]@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.<ref>:[password]@aws-0-<region>.pooler.supabase.com:5432/postgres"

# ─── Auth.js v5 — 🔴 BẮT BUỘC ────────────────────────────────────────────────
NEXTAUTH_URL="http://localhost:3000"           # prod: https://satarobo.vn
NEXTAUTH_SECRET="<openssl rand -base64 32>"
# ⚠️ DỰ ÁN CHỈ DÙNG `NEXTAUTH_SECRET`. ĐỪNG thêm `AUTH_SECRET` ở BẤT KỲ môi trường
# nào: `next-auth/lib/env.js` đọc `AUTH_SECRET ?? NEXTAUTH_SECRET` — AUTH_SECRET
# THẮNG, nên chỉ cần nó tồn tại với giá trị khác là MỌI phiên chết ngay lập tức,
# không một dòng cảnh báo. (Đúng cái đã gây sự cố 22/07→04/08/2026.)
# AUTH_TRUST_HOST=true là BẮT BUỘC sau proxy host-based — tin X-Forwarded-Host,
# nếu không thì callback trả về sai host.
AUTH_TRUST_HOST="true"

# ─── App ─────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"    # prod: https://satarobo.vn
NODE_ENV="development"

# =============================================================================
# FEATURE FLAGS (lib/flags.ts) — 🔴 QUYẾT ĐỊNH "deploy = local"
# Giá trị dưới đây = KHỚP đúng localhost hiện tại. Đổi giá trị = bật/tắt tính năng.
# (Flag đọc RUNTIME → phải có trên env của bản đang chạy, không chỉ lúc build.)
# =============================================================================
PORTAL_V2_ENABLED="true"       # Cổng phụ huynh SataUI mới (local ĐANG bật)
SCORM_ENABLED="true"           # Học liệu buổi SCORM + slide PDF (local ĐANG bật)
EVAL_V2_ENABLED="false"        # Phiếu đánh giá buổi SESSION_EVAL + khảo sát trung tâm
SESSION_LIFECYCLE_V2="false"   # "Hoàn tất buổi" (state machine, thay checklist 9 mục)
MEDIA_SIGNED_URL="false"       # Ảnh lớp qua presigned R2 (TTL ngắn) thay URL công khai
RBAC_V2_ENABLED="false"        # can() đọc quyền động từ DB (mặc định: matrix tĩnh v1)
SCOPE_SHADOW_ENABLED="false"   # P3/US-12: chạy shadow resolver dataScope đo bằng orgUnitId (chỉ ghi log, KHÔNG đổi quyền)
CONVERT_V2_ENABLED="true"      # Convert per-child (ON mặc định — entry DUY NHẤT; "false" chỉ để tắt khẩn cấp)
COMMON_LOGIN_ENABLED="true"    # Login chung satarobo.vn/login → redirect theo role (ON mặc định)
DISPATCHER_ENABLED="true"      # Cron xử lý DomainEvent outbox (ON mặc định)
# G-D — khoá endpoint nhận phiếu nhập khách: đòi đăng nhập + leads:create.
# ⚠️ Ba điều kiện của bản 21/08 ("để trống cho tới khi…") ĐÃ HẾT HẠN: 22/08 chủ dự án
# chốt dời hẳn sang satarobo.vn/nhap-khach-hang và biểu mẫu tĩnh ẩn danh đã bị xoá,
# nên cờ này đảo mặc định OFF → ON. Vẫn còn một việc NGƯỜI phải làm, không phải code:
# báo marketing/sale-admin địa chỉ mới và rà mọi nơi còn trỏ tới biểu mẫu cũ (QR,
# quảng cáo, chữ ký email) — chưa báo thì họ gặp trang đăng nhập mà không hiểu vì sao.
LEAD_INTAKE_REQUIRE_AUTH=""    # mặc định BẬT (bắt buộc đăng nhập). Chỉ đặt "false" nếu
                               # phải dựng lại biểu mẫu nhập khách công khai — 22/08/2026
                               # biểu mẫu đó đã nghỉ, thay bằng satarobo.vn/nhap-khach-hang.

# ─── Site Sale (sale.satarobo.vn) — ĐÃ GỠ 22/09/2026 ────────────────────────
# Không còn biến nào. Host sale.satarobo.vn nay LUÔN 307 về
# admin.satarobo.vn/nhap-khach-hang — giữ nhánh đó vì quảng cáo cũ, mã QR đã in,
# chữ ký email và RedirectURL của MISA (/thank-you) vẫn trỏ vào đây.
# Phần việc của site Sale đã dựng trong admin: "Việc hôm nay" ở /dashboard,
# "Tra cứu" ở /tra-cuu, nút nhắn Zalo trên màn chi tiết lead.

# ─── Q41 — SSO đa subdomain + cổng login chung (mặc định TẮT; bật SAU flip RBAC) ──
# ⚠️ THỨ TỰ BẮT BUỘC: (1) tách sale.satarobo.vn khỏi zone .satarobo.vn → (2) bật
# AUTH_COOKIE_DOMAIN (F2) → (3) bật NEXT_PUBLIC_COMMON_LOGIN + COMMON_LOGIN_AT_ROOT (F1+F4).
# Bật (2)/(3) khi CHƯA tách sale = cookie session lộ sang host tĩnh công khai.
# ⚠️ AUTH_COOKIE_DOMAIN phải là biến **Non-sensitive** trên Vercel: biến Sensitive
# KHÔNG tồn tại lúc build, mà middleware (edge) được build-time inline ⇒ middleware
# và server lệch tên cookie → đá vô hạn về /login. Tên cookie nay đã kèm tên môi
# trường (lib/auth.ts) nên hai môi trường không xoá cookie của nhau được nữa.
AUTH_COOKIE_DOMAIN=""          # F2 — domain cookie SSO (VD ".satarobo.vn"). TRỐNG=OFF (host-only). Bật → đổi tên cookie → mọi user re-login 1 lần.
COMMON_LOGIN_AT_ROOT="false"   # F4 — satarobo.vn/login SERVE form thật thay vì 307 sang admin. Chỉ bật SAU AUTH_COOKIE_DOMAIN.
NEXT_PUBLIC_COMMON_LOGIN="false" # F1 — logout đưa về cổng chung satarobo.vn/login (bật đồng bộ COMMON_LOGIN_AT_ROOT). TẮT=logout giữ host hiện tại (hành vi cũ).
NEXT_PUBLIC_LOGIN_URL=""       # F1 — override URL cổng login khi logout (staging/preview). TRỐNG=tự suy theo host.

# ─── Cron (Vercel Cron gọi /api/cron/*) — 🔴 set trước go-live ───────────────
# Route cron kiểm tra header: Authorization: Bearer <CRON_SECRET>. Vercel Cron tự gửi.
CRON_SECRET=""

# ─── Tracking (GA4 + Meta Pixel/CAPI) ────────────────────────────────────────
NEXT_PUBLIC_META_PIXEL_ID=""
META_CAPI_TOKEN=""
NEXT_PUBLIC_GA4_ID=""
GA4_API_SECRET=""

# ─── Meta CRM / Messenger / Ads (Phase R1) — đặt giá trị thật ở .env.local + Vercel ─
META_APP_ID=""
META_APP_SECRET=""             # App → Settings → Basic → App Secret (ký X-Hub-Signature-256)
META_PAGE_ID=""
META_PAGE_ACCESS_TOKEN=""      # reply Messenger + Ads Insights. CẦN quyền `pages_messaging`
                               # thì nút "Trả lời" mới gửi được ra khách — token chỉ có
                               # quyền Insights vẫn đọc được số liệu quảng cáo nhưng Meta
                               # sẽ từ chối Send API (hệ thống ghi FAILED + báo người dùng).
                               # ⚠️ Token này chỉ dùng được cho ĐÚNG Page `META_PAGE_ID`.
                               # Hội thoại thuộc Page khác sẽ bị chặn (META_SAI_PAGE), KHÔNG
                               # gửi nhầm bằng token Page khác — xem lib/crm/meta-messenger-provider.ts.
META_AD_ACCOUNT_ID=""
META_MESSENGER_LIVE="false"    # S-2b — DỰ PHÒNG cho máy dev. Công tắc chính là SystemSetting
                               # `messenger.sendLive` (/admin/cau-hinh-van-hanh — tắt gấp không
                               # cần deploy). Cả hai tắt ⇒ tin "Trả lời" ghi sổ trạng thái
                               # SIMULATED và giao diện nói thẳng KHÁCH KHÔNG NHẬN GÌ.

# ─── Email (Resend) ──────────────────────────────────────────────────────────
RESEND_API_KEY="re_xxxxx_get_from_resend_com_dashboard"
RESEND_FROM_EMAIL="Sata Robo <noreply@satarobo.vn>"
RESEND_REPLY_TO="info@satarobo.vn"
EMAIL_FROM="Sata Robo <noreply@satarobo.vn>"
CONSULT_NOTIFICATION_TO=""     # email nhận thông báo đăng ký tư vấn (form public)

# ─── Storage (Cloudflare R2) — 🔴 cần cho upload ảnh/tài liệu/SCORM ──────────
# ⚠️ Bucket này CÔNG KHAI: nó gắn custom domain R2_PUBLIC_URL (cdn.satarobo.vn),
#    nên MỌI object trong đó tải được vô danh qua https://cdn.satarobo.vn/<key>.
#    Chỉ để file được phép công khai (ảnh trang web, tài liệu public, SCORM…).
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME="satarobo-uploads"
R2_PUBLIC_URL="https://cdn.satarobo.vn"

# Bucket RIÊNG cho ảnh đính kèm trong chat (US-11) — dùng CHUNG credential R2 ở trên.
# Vì sao phải tách khỏi R2_BUCKET_NAME: ảnh chat là ảnh trẻ em trong nhóm lớp, bảo
# đảm đã ký là "bucket private · signed URL sống 5 phút · ảnh của tin đã gỡ → 403".
# Nếu ảnh nằm trong bucket có custom domain thì signed URL VÔ NGHĨA — signed URL
# chứa nguyên key, người nhận chỉ cần ghép https://cdn.satarobo.vn/<key> là có bản
# vĩnh viễn, không hạn giờ, tải được cả sau khi tin đã bị gỡ.
# 🔴 TUYỆT ĐỐI KHÔNG điền tên bucket đang có R2_PUBLIC_URL (tức đừng đặt bằng
#    R2_BUCKET_NAME) — code sẽ từ chối và trả 503 thay vì âm thầm dùng bucket công khai.
# Bucket phải: (1) tạo mới trên R2, (2) KHÔNG bật Public Access / KHÔNG gắn custom domain.
# Chưa đặt biến này → luồng ảnh chat trả 503 STORAGE_NOT_CONFIGURED (fail closed,
# KHÔNG fallback sang bucket công khai). Chat chữ vẫn chạy bình thường.
R2_CHAT_BUCKET_NAME=""

# ─── Zalo OA / ZNS — thông báo điểm danh + nhắc công nợ ─────────────────────
# Thiếu token → tự fallback EMAIL (an toàn). Bật ZALO_LIVE="true" để gọi API thật.
ZALO_OA_ID="40213330288531842"
ZALO_OA_ACCESS_TOKEN=""
ZALO_LIVE="false"
ZALO_ZNS_TEMPLATE_ATTENDANCE=""
ZALO_ZNS_TEMPLATE_DEBT=""
# BGĐ 31/07 — template ZNS thông báo cấp tài khoản (TK + link, KHÔNG kèm mật khẩu).
# Mẫu ZBS `616899` tạo 01/08/2026, tham số `name`(30) + `login_id`(15) — xem bảng
# khai ở lib/zalo/templates.ts. ⚠️ ĐANG CHỜ ZALO DUYỆT: đặt giá trị trước khi
# duyệt xong = mọi cú gửi hỏng mà provision.ts nuốt lỗi nên im lặng. Để trống thì
# hệ thống tự SKIP an toàn.
ZALO_ZNS_TEMPLATE_ACCOUNT=""
# AUTH-SĐT P5 — công tắc ngắt đường TỰ ĐỘNG cấp tài khoản phụ huynh theo SĐT
# (xác nhận đơn CONFIRMED + webhook SePay). Mặc định BẬT; để ngắt phải là đúng
# chuỗi `false` (viết hoa một chữ là vẫn chạy — xem lib/flags.test.ts).
# KHÔNG chắn các form nhân viên tự bấm; chỉ chắn đường chạy không có người duyệt.
# Kéo cờ khi: /kich-hoat chưa nhận SĐT (P5 chưa lên môi trường đó), hoặc ZNS chết
# hàng loạt khiến phụ huynh nhận tài khoản mà không nhận được mã.
AUTH_PHONE_PROVISIONING="true"
# AUTH-SĐT P5 — xác nhận đơn / biên nhận cho khách KHÔNG có email (mẫu đã duyệt
# 616258 "Học phí + tài khoản"). ⚠️ Đối chiếu TÊN THAM SỐ thật của mẫu trước khi
# đặt giá trị (bài học PR #77: mẫu Xác thực bắt tên `otp`, không phải tên tự đặt).
ZALO_ZNS_TEMPLATE_PAYMENT=""
# Auto-refresh (lib/zalo/token.ts): cấp 3 biến dưới (OAuth v4 1 lần) → tự xoay token.
ZALO_APP_ID=""
ZALO_APP_SECRET=""
ZALO_OA_REFRESH_TOKEN=""
ZALO_OA_TOKEN=""               # legacy (giữ tương thích)

# ─── Kế toán MISA (lib/misa) — đồng bộ hóa đơn/biên lai ─────────────────────
# Thiếu creds hoặc MISA_LIVE≠"true" → chạy chế độ stub (không gọi API thật).
MISA_API_URL=""
MISA_CLIENT_ID=""
MISA_CLIENT_SECRET=""
MISA_LIVE="false"

# ─── OTP (kích hoạt/quên mật khẩu phụ huynh) ────────────────────────────────
# P4: provider chọn theo LOẠI target (SĐT → Zalo ZNS, email → Resend) — env
# OTP_PRIMARY_PROVIDER đã GỠ (trước đây bị code nuốt im lặng). Tham số vận hành
# (TTL, cooldown, trần ngày, template ZNS…) nằm ở SystemSetting nhóm otp.* —
# đổi qua /admin/cau-hinh-van-hanh, không phải env. Xem docs/otp-service.md.
AUTH_ZNS_DEGRADED=""           # "true" = break-glass: ZNS chết hàng loạt → OTP tới SĐT đi thẳng email dự phòng

# ─── Lead webhooks — shared-secret theo từng nguồn ──────────────────────────
# Để trống = stub (không xác thực, chỉ dev). Đặt secret trước go-live.
WEBHOOK_FACEBOOK_VERIFY_TOKEN=""   # Meta → Webhooks → Leadgen (verify handshake)
WEBHOOK_FACEBOOK_SECRET=""
# Chữ ký X-Hub-Signature-256 (leadgen + messenger) verify bằng META_APP_SECRET ở trên.
WEBHOOK_ZALO_SECRET=""
WEBHOOK_GOOGLE_FORM_SECRET=""
# quatang.edu.vn (P3): Apps Script `doPost` của sheet gọi sang
# /api/public/webhook/quatang kèm header `x-webhook-secret`.
WEBHOOK_QUATANG_SECRET=""

# ─── Mirror form Sale sang MISA — ĐÃ GỠ 29/08/2026 ──────────────────────────
# Chủ dự án bỏ hẳn CRM của MISA. Toàn bộ biến `MISA_WEBFORM_*` / `MISA_FIELD_*`
# đã xoá khỏi mã nguồn; nếu môi trường nào còn đặt thì gỡ đi, chúng không còn
# được đọc. (Sync MISA AMIS cho KẾ TOÁN là việc KHÁC, vẫn còn: MISA_CLIENT_ID /
# MISA_CLIENT_SECRET / MISA_API_URL / MISA_LIVE.)
# BGĐ 31/07 — SePay đọc biến động số dư ngân hàng → POST /api/public/webhook/sepay
# để TỰ XÁC NHẬN đơn. SePay gửi header `Authorization: Apikey <key>`.
# Thiếu biến này → webhook TỪ CHỐI TẤT CẢ (an toàn khi chưa cấu hình).
#
# ⚠️ GIÁ TRỊ NÀY PHẢI TRÙNG TUYỆT ĐỐI với ô API Key trong my.sepay.vn → Webhooks →
# (webhook của satarobo.vn) → Kiểu xác thực = API Key. Lệch một ký tự = 401, SePay
# retry 5 lần rồi BỎ LUÔN giao dịch (sự cố 06→08/08/2026: mất 4 giao dịch ~26,8tr).
# Không cần thêm chữ "Apikey" hay dấu nháy vào đây — nếu lỡ dán kèm thì hàm
# `normalizeSepayKey` vẫn bóc ra, nhưng cứ dán key trần cho sạch.
# Bị 401 thì xem cột "Response Body" trong nhật ký webhook của SePay:
#   NO_ENV     → môi trường đang chạy chưa có biến này (kiểm tra đã redeploy chưa)
#   NO_HEADER  → bên SePay chưa bật xác thực API Key cho webhook
#   MISMATCH   → hai bên khác key (chi tiết độ lệch xem /admin/bien-dong-so-du)
SEPAY_WEBHOOK_API_KEY=""

# ─── payOS — cổng QR/link thanh toán + webhook đối khớp theo phiếu thu ───────
# 03/08. Webhook về POST /api/public/webhook/payos (chữ ký HMAC-SHA256 nằm TRONG body).
# THIẾU 3 biến đầu HOẶC PAYOS_LIVE≠"true" → CHẾ ĐỘ MÔ PHỎNG: không gọi API payOS,
# link/QR trả về mang tiền tố `SIMULATED-`; webhook chỉ nhận payload không chữ ký
# (hoặc chữ ký `SIMULATED-…`) để nghiệm thu mà không đụng tiền thật.
PAYOS_CLIENT_ID=""
PAYOS_API_KEY=""
PAYOS_CHECKSUM_KEY=""
PAYOS_LIVE="false"             # "true" = gọi API payOS THẬT (chỉ bật khi đã có credential)

# ─── Rate limiting (Upstash Redis / Vercel KV) — optional (fallback in-memory) ─
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
# Vercel KV inject sẵn KV_REST_API_URL / KV_REST_API_TOKEN — code đọc cả 2 tên.
KV_REST_API_URL=""
KV_REST_API_TOKEN=""

# ─── Tuân thủ / lưu trữ dữ liệu ─────────────────────────────────────────────
RETENTION_DAYS=""              # số ngày giữ dữ liệu nhạy cảm (lib/compliance/retention.ts)

# ─── Sentry error tracking — optional (SDK no-op nếu thiếu DSN) ──────────────
NEXT_PUBLIC_SENTRY_DSN=""
SENTRY_DSN=""
SENTRY_ORG=""
SENTRY_PROJECT=""
SENTRY_AUTH_TOKEN=""           # upload source maps lúc build (CI/Vercel)

# ─── Supabase JS (optional — chỉ cần nếu dùng Supabase client trực tiếp) ─────
# Thường được Supabase↔Vercel integration tự inject; app chính đọc DB qua Prisma.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# Chat realtime (US-02) — 2 biến dưới là SERVER ONLY, TUYỆT ĐỐI không NEXT_PUBLIC_.
SUPABASE_JWT_SECRET=""         # SERVER ONLY — ký JWT HS256 cho client subscribe private channel
SUPABASE_SERVICE_ROLE_KEY=""   # SERVER ONLY — server broadcast vào Realtime; cấm bundle client

# Đợt E (22/08/2026) — chính sách "dùng chung lead trong cơ sở".
# Để TRỐNG (mặc định) = lead ĐỘC QUYỀN: mỗi lead chỉ người phụ trách + người có
# leads:view-all xem được. Chủ dự án chốt Q8 (21/08), đảo BGĐ câu 10 ký 10/07.
# Cột Lead.isSharedWithTeam và toàn bộ dữ liệu VẪN CÒN — đặt "true" + redeploy là
# tính năng chạy lại y như cũ, không cần revert code, không mất dữ liệu.
LEAD_SHARING_ENABLED=""

# ─── Tổng đài OmiCall (trục gọi điện + ghi âm) — cờ OMICALL_ENABLED mặc định OFF ──
# 🔴 CHƯA CHẠY ĐƯỢC cho tới khi có VĂN BẢN NHÀ CUNG CẤP (cổng CH-3 của spec):
#    endpoint production thật (tài liệu hiện còn để host staging public-v1-stg.omicrm.com),
#    hạn mức + đơn giá ASR, cước viễn thông, chính sách lưu trữ tệp ghi âm và có tải
#    về được hay không, cơ chế xác thực webhook (có chữ ký hay chỉ Api-key).
OMICALL_ENABLED=""             # "true" = bật trục gọi điện. Mặc định OFF → route trả 404.
OMICALL_API_BASE=""            # endpoint PRODUCTION (chờ văn bản — KHÔNG dùng host staging)
OMICALL_API_KEY=""             # BÍ MẬT — không hardcode, không log giá trị (luật cứng #9)
OMICALL_TENANT=""
OMICALL_LIVE=""                # dự phòng cho SystemSetting `calls.live`; setting THẮNG
OMICALL_WEBHOOK_SECRET=""      # bí mật dùng chung cho webhook CDR. THIẾU trên production = 503
OMICALL_WEBHOOK_SIGNING_SECRET="" # nếu ĐẶT thì chữ ký HMAC-SHA256 thành BẮT BUỘC (chờ TQ-5)
NEXT_PUBLIC_OMICALL_SDK_URL="" # chỉ dùng cho TRANG THỬ app/(admin)/admin/_spike/omicall

# Bucket RIÊNG cho tệp GHI ÂM cuộc gọi — dùng CHUNG credential R2 ở trên.
# 🔴 TUYỆT ĐỐI KHÔNG điền tên bucket đang có R2_PUBLIC_URL (đừng đặt bằng R2_BUCKET_NAME):
#    bucket đó phát công khai qua cdn.satarobo.vn, mọi object tải được VÔ DANH — ghi âm
#    giọng phụ huynh nằm ở đó là rò rỉ vĩnh viễn, không thu về được. Code sẽ TỪ CHỐI.
# Cũng không được trùng R2_CHAT_BUCKET_NAME hay R2_ELEARNING_BUCKET_NAME: ghi âm có lịch
# xoá bắt buộc (12 tháng) còn ảnh lớp/video đào tạo thì không — dùng chung là để job dọn
# của bên này xoá nhầm tệp của bên kia.
# Bucket phải: (1) tạo mới trên R2, (2) KHÔNG bật Public Access / KHÔNG gắn custom domain.
# Chưa đặt → luồng nghe ghi âm trả 503 STORAGE_NOT_CONFIGURED (fail closed).
R2_CALL_BUCKET_NAME=""

# Bucket RIÊNG cho tệp HOÁ ĐƠN ĐIỆN TỬ (PDF/XML kế toán tải lên — docs/ke-toan-hoa-don/PLAN.md §6).
# 🔴 KHÔNG điền tên bucket công khai (R2_BUCKET_NAME) — hoá đơn mang MST, địa chỉ, email khách.
#    Cũng không trùng bucket chat / đào tạo / ghi âm. Code TỪ CHỐI (getHoaDonBucket ném lỗi).
# Bucket: tạo mới, KHÔNG Public Access, KHÔNG custom domain; CORS: `pnpm exec tsx scripts/apply-r2-cors.ts hoa-don`.
# Chưa đặt → màn hoá đơn báo "Kho lưu hoá đơn chưa cấu hình" (fail closed). Prod: satarobo-hoa-don · test: satarobo-hoa-don-test.
R2_INVOICE_BUCKET_NAME=""

# ─── ZaloCRM (trục Zalo cá nhân nhúng) — cờ ZALOCRM_ENABLED mặc định OFF ────────
# OFF nghĩa là KHÔNG TỒN TẠI với người dùng: mục sidebar "Zalo CRM" ẩn, trang
# /admin/zalo-crm trả 404, webhook /api/webhooks/zalocrm/<org> trả 404 (ZaloCRM giữ
# tin trong outbox của nó và gửi lại — KHÔNG mất tin). Bật SAU CÙNG trong GĐ4, khi
# máy chủ fork đã chạy và ánh xạ cơ sở ↔ orgCode đã khai.
# Rollback = đặt "false" + redeploy; chữ đã nhận nằm ở bảng Inbox* nên không mất gì.
ZALOCRM_ENABLED=""             # "true" = bật trục ZaloCRM. Gõ gần đúng ("1"/"TRUE") vẫn TẮT.
ZALOCRM_BASE_URL=""            # gốc API máy chủ ZaloCRM (server→server), vd https://zalo-api.satarobo.vn
ZALOCRM_APP_URL=""             # gốc giao diện nhúng vào iframe, vd https://zalo.satarobo.vn
                               # 🔴 Cũng là origin DUY NHẤT được tin khi nhận postMessage.
                               # Khai sai/để trống = khung nhúng nhận lệnh từ tab bất kỳ.
                               # Nhớ khai kèm frame-src trong next.config.ts, nếu không
                               # CSP (khi hết Report-Only) sẽ chặn iframe.
ZALOCRM_SSO_SECRET=""          # BÍ MẬT — khoá HS256 ký token SSO 60 giây. Không log, không
                               # hardcode (luật cứng #9). Thiếu ⇒ ném MISSING_SECRET, KHÔNG
                               # ký token rỗng.
ZALOCRM_WEBHOOK_SECRETS=""     # BÍ MẬT — JSON {"<orgCode>":"<secret>"}, MỖI org một khoá
                               # riêng: dùng chung một khoá thì lộ ở một cơ sở là mở cửa
                               # cho mọi cơ sở. THIẾU trên production = 503 (fail closed),
                               # chữ ký rỗng/sai = 401 — KHÔNG có nhánh "không có khoá thì
                               # cho qua" như webhook OmiCall.
ZALOCRM_API_KEYS=""            # BÍ MẬT — JSON {"<orgCode>":"<api key>"} để Sata gọi ngược
                               # sang ZaloCRM. Chỉ cần từ GĐ3 (đồng bộ nick / gửi tin).

# Hộp thư đa kênh (lib/inbox/*, /sale/hop-thu) — cờ 2-phase của CẢ tính năng.
# ⚠️ Cờ MỚI KHAI 06/09/2026, CHƯA nối vào màn nào: đặt gì lúc này cũng không đổi hành
# vi. Người nối cờ về sau đọc JSDoc isInboxEnabled() trong lib/flags.ts trước khi merge —
# hộp thư có thể đang chạy thật, gate nhầm là đóng nó ngay dưới chân người trực chat.
# Công tắc GỬI THẬT của từng kênh KHÔNG nằm ở đây mà ở SystemSetting inbox.*Live
# (màn Cấu hình vận hành) — thứ cần tắt gấp thì không được đòi deploy lại.
INBOX_ENABLED=""               # "true" = bật hộp thư đa kênh. Mặc định OFF.
# ─── Web Push (VAPID) — optional, chỉ cần khi bật kênh thông báo đẩy cho nhân viên ──
# Từ Đợt 4 (08/09/2026) CẢ BA biến đều được đọc thật: hai biến đầu ở `lib/push/engine.ts`
# (`docCauHinhVapid`), khoá công khai còn được đọc thêm ở màn đăng ký thiết bị. Thiếu hoặc sai
# hình dạng MỘT trong ba ⇒ mỗi lượt cron `push-outbox` trả `reason: "NO_VAPID"` và bỏ CẢ LƯỢT,
# không dòng nào bị tiêu attempt — cố ý, để một lần dán nhầm env không đốt sạch hàng đợi.
# Sinh cặp khoá: pnpm exec tsx scripts/tao-khoa-vapid.ts  (in ra màn hình, KHÔNG ghi file).
#
# CHỈ MỘT biến giữ khoá công khai. Cố ý KHÔNG có `VAPID_PUBLIC_KEY` riêng cho server —
# hai biến cùng một giá trị là đường mòn dẫn tới ngày xoay khoá sửa sót một bên, và khi đó
# MỌI push chết 403 VapidPkHashMismatch mà không typecheck/lint/build nào bắt được. Repo đã
# có vết y hệt: AUTH_SECRET vs NEXTAUTH_SECRET (lib/auth.ts).
#
# ⚠️ Trên Vercel, biến dưới PHẢI là Non-sensitive: biến Sensitive không tồn tại lúc build,
# mà NEXT_PUBLIC_ được nhúng vào bundle LÚC BUILD ⇒ để Sensitive là trình duyệt nhận
# undefined, không đăng ký được thiết bị nào, và server không thấy lỗi gì.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""           # SERVER ONLY — ký JWT VAPID; để Sensitive trên Vercel
# mailto: hoặc https:. Phải là hộp thư CÓ NGƯỜI ĐỌC — push service dùng nó để liên hệ khi
# hạ tầng của ta gây sự cố; đặt địa chỉ không ai đọc thì ngày bị bóp kênh, triệu chứng duy
# nhất ta thấy là push im lặng.
VAPID_SUBJECT=""               # ví dụ: mailto:it@satarobo.vn
#
# Công tắc bật/tắt kênh KHÔNG nằm ở đây: `push.webPushEnabled` trong
# /admin/cau-hinh-van-hanh (mặc định TẮT). Env chỉ giữ khoá bí mật — đúng nếp
# `zalo.znsLive` / `chat.znsNotifyEnabled`.

# ─── Cổng dữ liệu agent (tài liệu CEO 25/09/2026) ─────────────────────────────────────────
# Runbook: docs/cong-du-lieu-agent/README.md
# Ba khoá, MỖI MÔI TRƯỜNG MỘT BỘ RIÊNG (test ≠ production — spec §4.6). Để Sensitive trên
# Vercel. Sinh bằng: openssl rand -base64 32 (hoặc: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
#
# Pepper băm mật khẩu client + token của agent. THIẾU hoặc < 32 ký tự ⇒ cổng từ chối MỌI
# lượt gọi (fail closed). ĐỔI giá trị ⇒ mọi mật khẩu/token đang có chết ngay — phải sinh lại.
AGENT_GATEWAY_PEPPER=""
# Pepper RIÊNG cho mã băm SĐT (`sdt_ma_hoa`) trả cho agent — KHÔNG dùng chung với pepper trên.
# Chưa dùng ở Đợt 0 (chưa có công cụ lead); khai trước để Đợt 3 không phải chờ.
AGENT_PII_PEPPER=""
# Khoá AES-256-GCM (32 byte, base64 hoặc hex 64 ký tự) mã hoá bí mật xác thực 2 lớp (TOTP)
# của người giữ quyền cổng. THIẾU ⇒ không ai cài/dùng được 2FA ⇒ không thao tác được màn
# quản trị cổng. MẤT/ĐỔI ⇒ mọi người phải cài lại 2FA.
TOTP_ENCRYPTION_KEY=""
