# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — .env.example được commit, còn lại không
.env
.env.local
.env*.local
.env.development
.env.production
.env.staging
.env.test

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma

# Backup files (NEVER commit)
*.bak
*.backup
*.swp
*.swo
*~
Thumbs.db

# Sensitive files (extra safety)
*.key
*credentials*
*-secret*
secret-*
*.sql.bak
*.dump

# Claude Code per-user settings (project-wide config trong rules/skills/commands IS committed)
.claude/settings.local.json
.claude/cache/
.claude/.logs/

# Phase 4.UI.FIX.3 — Legacy source reference (do NOT commit)
/_legacy-sources/
/_temp-uploads/

